node_modules/
dist/
build/
*.local
.DS_Store

# per-workspace agent config/memory (user-specific; generated on demand in-app).
# Root-anchored so the bundled examples/.eveglyph/ starter ships, but a real
# workspace's own .eveglyph/ at the repo root stays private.
/.eveglyph/

# secrets — never commit
.env
.env.*
*.key
*.pem
*.p12
*.pfx

# editor / tooling local config (machine-specific paths)
.claude/

# dev scratch / diagnostics
dev-server*.log
_monitor_baseline.txt
*.tmp
zz-*

# generated PDF exports from local Typst testing — regenerable output, not source
/demo/
